#ifndef PDESOLVER_SOLVER_CONFIG_MESHCONFIG_HPP
#define PDESOLVER_SOLVER_CONFIG_MESHCONFIG_HPP

#include "core/Types.hpp"

#include <string>

namespace pdesolver {
	namespace solver {
		namespace config {

			struct MeshConfig {

				std::string file;

			}; // struct MeshConfig

		} // namespace config
	} // namespace solver
} // namespace pdesolver

#endif

