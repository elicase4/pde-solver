#ifndef RESIDUUM_SOLVER_CONFIG_MESHCONFIG_HPP
#define RESIDUUM_SOLVER_CONFIG_MESHCONFIG_HPP

#include "core/Types.hpp"

#include <string>

namespace residuum {
	namespace solver {
		namespace config {

			struct MeshConfig {

				std::string file;

			}; // struct MeshConfig

		} // namespace config
	} // namespace solver
} // namespace residuum

#endif

