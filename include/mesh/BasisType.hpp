#ifndef RESIDUUM_MESH_BASISTYPE_HPP
#define RESIDUUM_MESH_BASISTYPE_HPP

namespace residuum {
	namespace mesh {

		enum class BasisType {
			Lagrange,
			Spline
		}; // enum class BasisType

	} // namespace mesh
} // namespace residuum

#endif
