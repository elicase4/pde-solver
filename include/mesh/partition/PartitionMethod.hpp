#ifndef RESIDUUM_MESH_PARTITION_PARTITIONMETHOD_HPP
#define RESIDUUM_MESH_PARTITION_PARTITIONMETHOD_HPP

#include "core/Types.hpp"
#include "mesh/Mesh.hpp"

namespace residuum {
	namespace mesh {
		namespace partition {

			enum class PartitionMethod {RCB, METIS}; // enum class PartitionMethod

		} // namespace partition
	} // namespace mesh
} // namespace residuum

#endif
